# =====================================================================
# Custom postgresql.conf for TimescaleDB containers
# =====================================================================

# Use custom pg_hba.conf location
hba_file = '/etc/postgresql/pg_hba.conf'

# Listen on all interfaces (for Docker network)
listen_addresses = '*'

# Preload timescaledb extension
shared_preload_libraries = 'timescaledb'
