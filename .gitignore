# Secrets
.env

# Runtime data / persistent volumes (databases, message queues, metrics)
volumes/

# Build artifacts
node_modules/
**/node_modules/
dashboard/dist/
firmware/aeroponic-node/.pio/
.pio/
.pio_env/
firmware/aeroponic-node/.pio_env/

# OS / editor
.DS_Store
Thumbs.db
.idea/
*.log

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# Agent/runtime metadata
.kilo/

# Build artifacts / binaries
services/stream/stream-svc

# ML model weights (store in the models volume, not the repo)
*.pt

# TD3/PPO training artifacts
control-model-training/models/*.zip
control-model-training/models/*.pkl
control-model-training/aeroponic_td3_tensorboard/
control-model-training/results/*.png
control-model-training/results/*.csv
ppo-model-training/models/*.zip
ppo-model-training/models/*.pkl
ppo-model-training/aeroponic_ppo_tensorboard/
ppo-model-training/results/*.png
ppo-model-training/results/*.csv
